{% extends 'main/base.html.twig' %}

{% block title %}Вход{% endblock %}

{% block body %}

    <div class="container-fluid">
        <div class="row no-gutters justify-content-center">
            <div class="col-12 col-lg-4 col-md-4">
                <div class="form-login">
                    <form method="post">

                        <h1 class="h3 mb-4 font-weight-normal text-center font-open-sans" style="font-size: 40px;">Вход в систему</h1>

                        {% if error %}
                            <div class="alert alert-warning">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}

                        <label for="inputEmail" class="font-open-sans description-size">Email</label>
                        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control orange-shadow mb-1 border-orange" required autofocus>
                        <label for="inputPassword" class="font-open-sans description-size">Пароль</label>
                        <input type="password" name="password" id="inputPassword" class="form-control  border-orange orange-shadow mb-1" required>

                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                        {#
                            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                            See https://symfony.com/doc/current/security/remember_me.html

                            <div class="checkbox mb-3">
                                <label>
                                    <input type="checkbox" name="_remember_me"> Remember me
                                </label>
                            </div>
                        #}

                        <button class="btn btn-block orange-color btn-size font-amatic mt-2" type="submit">Войти</button>
                        <a class="btn btn-block btn-outline-danger red-color btn-size font-amatic mt-2 registration" href="{{ path('user_create') }}">Зарегистрироваться</a>
                    </form>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
